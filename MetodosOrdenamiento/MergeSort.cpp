#include <iostream>
using namespace std;

void Merge(vector<int>& v, int p, int q, int r)

int main(int argc, char *argv[]) {
	vector<int> vec = {5, 2, 4, 6, 1, 3};
	Merge<int>(vec);
	for(auto v : vec){
		cout << v << ' ';
	}
	return 0;
	return 0;
}

